% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations.R
\name{.fir_filter_vector}
\alias{.fir_filter_vector}
\title{Design-and-Apply a Bandpass or Lowpass FIR Filter to One Vector (internal)}
\usage{
.fir_filter_vector(
  x,
  sfreq,
  l_freq = NULL,
  h_freq = NULL,
  fir_window = "hamming",
  phase = "zero",
  pad = "reflect_limited"
)
}
\arguments{
\item{x}{Numeric vector. The signal to filter.}

\item{sfreq}{Numeric. Sampling rate in Hz.}

\item{l_freq}{Numeric or \code{NULL}. Lower passband edge in Hz.}

\item{h_freq}{Numeric or \code{NULL}. Upper passband edge in Hz.}

\item{fir_window, phase, pad}{Passed through to \code{.firwin_kernel()} /
\code{.overlap_add_filter()}. Defaults match \code{eeg_bandpass()}.}
}
\value{
Numeric vector, same length as \code{x}.
}
\description{
A single-vector counterpart to \code{\link{eeg_bandpass}} (see
R/filter1.R): builds the same freq/gain kernel specification
\code{eeg_bandpass()} does (bandpass when both \code{l_freq}/\code{h_freq}
are given, lowpass when only \code{h_freq} is given), then applies it with
the same \code{.firwin_kernel()} / \code{.overlap_add_filter()} primitives
\itemize{
\item without constructing a full \code{eeg} object, which is all
\code{\link{annotate_muscle}} needs for its two internal filtering steps
(110-140 Hz bandpass on each channel, 4 Hz lowpass on the combined
z-score trace). Highpass-only is intentionally not implemented - no
caller in this file needs it.
}
}
\keyword{internal}
