% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations.R
\name{.flip_short_runs}
\alias{.flip_short_runs}
\title{Flip Short Runs of a Given Value (internal)}
\usage{
.flip_short_runs(mask, min_samples, run_value, flip_to)
}
\arguments{
\item{mask}{Logical vector.}

\item{min_samples}{Numeric. Runs of \code{run_value} shorter than this
many samples are flipped.}

\item{run_value}{Logical. Which run value is subject to flipping.}

\item{flip_to}{Logical. What a too-short run of \code{run_value} becomes.}
}
\value{
Logical vector, same length as \code{mask}.
}
\description{
Run-length-encodes \code{mask} and flips any run equal to \code{run_value}
shorter than \code{min_samples} samples to \code{flip_to}. Used two ways
in this file: \code{\link{annotate_muscle}} uses it to fold short "good"
gaps into the surrounding flagged stretch (\code{run_value = FALSE,
flip_to = TRUE}); \code{\link{annotate_amplitude}} uses it to discard
flagged runs that don't last long enough to count (\code{run_value =
TRUE, flip_to = FALSE}).
}
\keyword{internal}
