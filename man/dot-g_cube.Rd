% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ica1.R
\name{.g_cube}
\alias{.g_cube}
\title{cube G-function and its derivative}
\usage{
.g_cube(x, fun_args = list())
}
\arguments{
\item{x}{Numeric vector (length n_samples) or matrix (components x
n_samples): the current unmixing projection \code{w'X} / \code{WX}.}

\item{fun_args}{Named list; only \code{alpha} is read (default \code{1.0}),
matching sklearn's \code{fun_args.get("alpha", 1.0)}. Must lie in
\code{[1, 2]}, per \code{FastICA}'s own parameter validation - not
re-validated here.}
}
\value{
A list with \code{gx} (same shape as \code{x}) and \code{g_x}
(already reduced: a single number for vector input, one value per row
for matrix input).
}
\description{
Mirrors sklearn's \code{_cube()} (\code{_fastica.py:167-168}). Same
always-reduced behavior as \code{.g_exp()} - see its docs.
}
\keyword{internal}
