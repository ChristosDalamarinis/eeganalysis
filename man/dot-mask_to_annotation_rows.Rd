% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotations.R
\name{.mask_to_annotation_rows}
\alias{.mask_to_annotation_rows}
\title{Convert a Boolean Mask into Annotation Rows (internal)}
\usage{
.mask_to_annotation_rows(mask, sfreq, description, channel = NA_character_)
}
\arguments{
\item{mask}{Logical vector, one element per sample.}

\item{sfreq}{Numeric. Sampling rate in Hz.}

\item{description}{Character scalar. Value for the \code{description}
column of every row produced.}

\item{channel}{Character scalar. Value for the \code{channel} column of
every row produced. Default \code{NA_character_} (not channel-specific).}
}
\value{
A data frame with columns \code{onset, duration, description,
  channel} (see \code{\link{.empty_annotations}}), zero rows if
\code{mask} has no \code{TRUE} runs.
}
\description{
Runs \code{\link{.mask_to_onsets_offsets}} on \code{mask} and converts
each run to seconds, assuming sample 1 falls at t = 0 (matching
\code{\link{new_eeg}}'s auto-generated \code{times}).
}
\keyword{internal}
