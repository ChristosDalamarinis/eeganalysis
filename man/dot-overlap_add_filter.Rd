% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter1.R
\name{.overlap_add_filter}
\alias{.overlap_add_filter}
\title{Apply FIR filter via overlap-add FFT convolution}
\usage{
.overlap_add_filter(x, h, phase = "zero", pad = "reflect_limited")
}
\arguments{
\item{x}{Numeric vector. The signal to filter (one channel).}

\item{h}{Numeric vector. The FIR kernel coefficients.}

\item{phase}{Character. "zero" (default) for zero-phase single-pass.}

\item{pad}{Character. Padding mode. Default "reflect_limited".}
}
\value{
Numeric vector of same length as x - the filtered signal.
}
\description{
Pipeline:
\enumerate{
\item Compute padding length: n_edge = max(min(n_h, n_sig) - 1, 0)
\item Select optimal FFT block size (cost-minimising power of 2)
\item Pad signal with reflect_limited by n_edge on each side
\item Overlap-add FFT convolution loop (block by block)
\item shift = (n_h-1)//2 + n_edge (zero-phase correction + unpadding in one)
\item Trim output back to original signal length
}
}
\keyword{internal}
