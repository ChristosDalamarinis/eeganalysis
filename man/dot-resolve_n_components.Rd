% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ica1.R
\name{.resolve_n_components}
\alias{.resolve_n_components}
\title{Resolve \code{n_components} to a concrete integer at fit time}
\usage{
.resolve_n_components(n_components, pca_explained_variance_)
}
\arguments{
\item{n_components}{The (already shape-validated) \code{new_ica()}
parameter: \code{NULL}, an integer, or a float in (0, 1).}

\item{pca_explained_variance_}{Numeric vector, decreasing order: the
\code{pca_explained_variance_} element returned by \code{.pca_whiten()}
for the \emph{full} (unsliced) PCA - one eigenvalue per available channel.}
}
\value{
A single integer, \code{>= 1} and \code{<= length(pca_explained_variance_)}.
}
\description{
\code{new_ica()} validates the \emph{shape} of \code{n_components} (NULL / an
integer != 1 / a float in (0, 1)) but can't resolve what it actually means
until fit time, since that depends on data it doesn't have yet - either
the total channel count (integer case) or the PCA explained-variance
profile (NULL / float case, both selecting components by cumulative
variance). This is that resolution step, called from \code{fit_ica()}
right after \code{.pca_whiten()}.
}
\keyword{internal}
