% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channel_info2.R
\name{plot_electrode_3d_spherical}
\alias{plot_electrode_3d_spherical}
\title{============================================================================
Plot 3D Electrode Layout on Scalp (BioSemi 64-Channel - Spherical)}
\usage{
plot_electrode_3d_spherical(
  channels = NULL,
  highlight_channels = NULL,
  show_labels = TRUE,
  color_by = "region",
  show_head = TRUE,
  head_radius = 87.54
)
}
\arguments{
\item{channels}{Character vector - channel names to plot. If NULL, plots all
64 standard 10-20/10-10 electrodes. Can also accept BioSemi A/B naming.}

\item{highlight_channels}{Character vector - specific channels to highlight
in a different color (optional)}

\item{show_labels}{Logical - whether to show electrode labels on the plot.
Default is TRUE.}

\item{color_by}{Character - how to color electrodes. Options:
\itemize{
\item "region" = color by brain region (frontal, central, parietal, occipital, temporal)
\item "single" = all one color (default blue)
Default is "region".
}}

\item{show_head}{Logical - whether to show the head outline. Default is TRUE.}

\item{head_radius}{Numeric - radius of head sphere in mm. Default is 87.54 mm
(calculated from 55 cm circumference).}
}
\value{
Returns a plotly object (interactive 3D plot viewable in RStudio)
}
\description{
Creates an interactive 3D visualization of electrode positions on a spherical
head model using OFFICIAL BioSemi 64-channel SPHERICAL coordinates.
}
\details{
This function visualizes electrode positions in 3D space using the official
BioSemi 64-channel electrode cap SPHERICAL coordinates (theta, phi), which are then
converted to Cartesian (x, y, z) for plotting.

\strong{Spherical Coordinate System:}
\itemize{
\item theta (inclination): range from +180deg to -180deg (at Cz, inclination = 0deg)
\itemize{
\item Positive inclinations = right side of head
\item Negative inclinations = left side of head
}
\item phi (azimuth): range from +90deg to -90deg
\itemize{
\item For positive inclinations: phi = angle from T8 (right ear)
\item For negative inclinations: phi = angle from T7 (left ear)
\item Positive = anti-clockwise, negative = clockwise
}
\item r (radius): 87.54 mm (from 55 cm head circumference)
}

\strong{Conversion to Cartesian:}
\itemize{
\item x = r * sin(theta) * cos(phi)
\item y = r * sin(theta) * sin(phi)
\item z = r * cos(theta)
}

\strong{Coordinate System:}
\itemize{
\item Origin (0, 0, 0) at center of head
\item X-axis: Left (-) to Right (+)
\item Y-axis: Back (-) to Front (+)
\item Z-axis: Bottom (-) to Top (+)
\item Nose points in +Y direction
}
}
\examples{
\dontrun{

  # Example 1: Plot all 64 electrodes
  plot_electrode_3d_spherical()

  # Example 2: Plot specific channels
  plot_electrode_3d_spherical(channels = c("Cz", "Fz", "Pz", "Oz", "T7", "T8"))

  # Example 3: Highlight midline electrodes
  plot_electrode_3d_spherical(highlight_channels = c("Fpz", "Fz", "Cz", "Pz", "Oz"))

  # Example 4: Different head size
  plot_electrode_3d_spherical(head_radius = 90)
}

}
\keyword{internal}
