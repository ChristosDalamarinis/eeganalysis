% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ica1.R
\name{plot_ica_topography}
\alias{plot_ica_topography}
\title{Plot an Independent Component's Scalp Topography}
\usage{
plot_ica_topography(ica, eeg, component, montage = NULL, ...)
}
\arguments{
\item{ica}{A fitted \code{eeg_ica} object (see \code{fit_ica()}).}

\item{eeg}{An \code{eeg} object. Used to read \code{eeg$montage} when
\code{montage} is not supplied directly, and passed through to
\code{plot_topography()}.}

\item{component}{Single integer, the component index to plot.}

\item{montage}{An object of class \code{'montage'}. If \code{NULL}
(default), \code{eeg$montage} is used.}

\item{...}{Additional arguments forwarded to \code{\link{plot_topography}}
(e.g. \code{interpolate_res}, \code{contour}, \code{col_palette}).}
}
\value{
Invisibly returns whatever \code{\link{plot_topography}} returns.
}
\description{
One-call convenience wrapper: computes a component's topography with
\code{\link{get_component_topography}} and draws it with
\code{\link{plot_topography}}. Requires a montage attached to \code{eeg}
(or passed explicitly) - see \code{\link{set_montage}},
\code{\link{create_montage}}.
}
\examples{
\dontrun{
  eeg <- set_montage(eeg, create_montage())
  ica <- fit_ica(new_ica(n_components = 0.95), eeg)
  plot_ica_topography(ica, eeg, component = 1)
}

}
\seealso{
\code{\link{get_component_topography}}, \code{\link{plot_topography}}
}
